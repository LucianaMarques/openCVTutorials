cmake_minimum_required(VERSION 2.8)
project( loadmodify )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( LoadModify loadmodify.cpp )
target_link_libraries( LoadModify ${OpenCV_LIBS} )
